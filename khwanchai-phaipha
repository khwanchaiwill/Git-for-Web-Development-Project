 [x] Add your TL as a collaborator
- [x] Clone this repo
- [x] Create a branch `git checkout -b 'firstName-lastName'`
  - [x] Add a file to the project called `yourFirstName-yourLastName`.txt. This file can contain anything.
  - [x] Add a file to the project called `yourFirstName-yourLastName`.txt. This should contain the link to your completed codepen from part 2 as well as the review questions/answers
  - [x] Run your usual git commands for adding/committing and pushing **Be sure to push to your branch!**
- [x] Create a Pull-Request to submit your work
  - [x] Use your own student fork as the base (compare across forks, base-fork -> master).
  - [x] Add your TL as a reviewer on the Pull-Request
- [x] TL then will count the Assignment as done by merging the HW back into master "STUDENT FORK".

<!-- Questions to Submit in today's file first-lastname.txt --> 
<!--
Copy the questions below into the first-lastname.txt file on your github repo - answer each question and then push your changes. 

    1. What is Semantic HTML? 
       Html meaning for content.
    2. What is HTML used for? 
       It's use for structur.
    3. What is an attribute and where do we put it? 
       name and values of the tag.
    4. What is the h1 tag used for? How many times should I use it on a page?
       h1 tag use for specific text size in HTML, I can use any time I want.
    5. Name two tags that have required attributes
       <a> tag <img> tag.
    6. What do we put in the head of our HTML document? 
       If use codepend we can put header first, If the other program we need to put <html> tag.
    7. What is an id?
       id is an attribute to select the spedific element.
    8. What elements can I add an id to? 
       the specific element that have meaning.
    9. How many times can I use the same id on a page? 
       Only 1 id.
    10. What is a class?
        An attribute to specific the element.
    11. What elements can I add a class to?
        Any element.
    12. How many times can I use the same class on a page? 
        Many as I want.
    13. How do I get my link to open in a new tab?
        add target="_blank" to the attribute link.
    14. What is the alt attribute in the image tag used for? 
        specific an alternate text for image.
    15. How do I reference an id?
        #(id name).

    16. What is the difference between a section and a div
        div do not have any meaning just use to wrap the tag together. Section is Semantic for Html that have meaning.
    17. What is CSS used for?
        To sytyle the Html.
    18. How to we select an element? Example - every h2 on the page
        h2{}.
    19. What is the difference between a class and an id? - Give me an example of when I might use each one
        The class to select the same content in element. id is to specific unique element. Example <section class="room",>
        <div id="longtail">
    20. How do we select classes in CSS?
        .<the name of class>
    21. How do we select a p element with a single class of “human””?
        p.human
    22. What is a parent child selector? When would this be useful?
        parent is the first selector element, the child is the second selector element. Use it when want to finding the children element
    23. How do you select all links within a div with the class of sidebar?
        .sidebar div a
    24. What is a pseudo selector?
        
    25. What do we use the change the spacing between lines?
        line-height
    26. What do we use to change the spacing between letters?
        letter-spacing: (size)px;
    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize?
        font-transform: values of what  I want to change  ;
    28. How do I add a 1px border around my div that is dotted and black?
        border: 1px ditted pink;
    29. How do I select everything on the page? 
        html{}
    30. How do I write a comment in CSS?
        /* comment */
    31. How do I find out what file I am in, when I am using the command line? 
        git status
    32. Using the command line - how do I see a list of files/folders in my current folder?
        ls
    33. How do I remove a file via the command line? Why do I have to be careful with this?
        rm filename, I have to careful because it forever remove. 
    34. Why should I use version control?
        To do the project easier and do not have save a different name when I want to a 
    35. How often should I commit to github?
        20 minutes as often as i make changed
    36. What is the command we would use to push our repo up to github?
        git push -u origin "khwanchai-phaipha"  
    37. Walk me through Lambda's git flow. 
        https://www.notion.so/GitFlow-step-by-step-a764163309754e23b1b9c4077b1653cc

Stretch Questions

    1. What is the difference between an inline element and a block element?
       inline element is the element to occupy in 
    2. What happens when an element is positioned absolutely? 
       The result will show the style in that element as we valued it.
    3. How do I make an element take up only the amount of space it needs but also have the ability to give it a width?
        <p>
    4. Name 3 elements that are diplay block by default, 2 elements that are display inline by default and 1 element that is display inline-block by default
       <p> <sention> <header>, <span> <a> <img>, <span> 
    5. In your own words, explain the box model. What is the "fix" for the box model, in other words, how do we make all elements respect the width we've given them? 
       The box model is the model for our page that how we want to be and what style we want,  Fix for the box model that I understand is to layout the border, 
       padding and also text how we want it to design, We use the values by put px; after the number of size we want.

mycodepen project : https://codepen.io/Paewill8428/pen/KKdZNgr
   --> 